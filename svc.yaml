apiVersion: v1
kind: Service
metadata:
  name: task_ff  # A valid metadata name
  labels:
    app: test_f
spec:
  selector:
    app: test_f
  ports:
    - name: http
      port: 80         # Service port (external)
      targetPort: 80   # Container port
      protocol: TCP
  type: LoadBalancer  # Correct type for external access
